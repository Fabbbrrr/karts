package com.raceface.wear.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Brand colours (ported from CSS variables in styles.css) ──────────────────
val RaceFacerGreen  = Color(0xFF00FF88)   // --accent: best times, connected
val RaceFacerAmber  = Color(0xFFFFAA00)   // normal last lap
val RaceFacerRed    = Color(0xFFFF4444)   // incidents, disconnect
val RaceFacerPurple = Color(0xFFB06BD6)   // personal best laps

// ── Position colours ──────────────────────────────────────────────────────────
val GoldP1          = Color(0xFFFFD700)
val SilverP2        = Color(0xFFC0C0C0)
val BronzeP3        = Color(0xFFCD7F32)

// ── Backgrounds ───────────────────────────────────────────────────────────────
val BackgroundBlack = Color(0xFF000000)
val SurfaceDark     = Color(0xFF1A1A1A)
val SurfaceDark2    = Color(0xFF252525)
val TextMuted       = Color(0xFF555555)
val TextMuted2      = Color(0xFF888888)
